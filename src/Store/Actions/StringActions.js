
export const startStringGen = (state) => {
    console.log("SRING GEN ACTIONS")
    const { chars, numChars, delimiter, numCases, output } = state
    if (output !== '') {
        console.log('ERROR: output not empty, submit clicked more than once')
    }
    let result = ''
    for ()
}