import React from 'react'
import { Container, Typography, Card } from '@material-ui/core'

const Header = () => {
    return(
        <Container>
            <Typography>
                <Card>
                    empty header
                </Card>
            </Typography>
        </Container>
    )
}

export default Header;